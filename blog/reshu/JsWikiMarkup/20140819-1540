Подзадача: сформировать путь для загрузки исходного текста.
Исходные данные:
* src_base_path передан при вызове скрипта со страницы html
* ?s=... в строке адреса
Выполнено.

Подзадача: получить содержимое исходного текста.
Использую jquery ajax.
Выведу пока исходный текст внутри тега <pre>.
Выполнено.

Уберу лишнюю отладку.

Теперь собственно я дошёл до форматирования текста.
Подзадача: разбить исходный текст на строки. Это несложно.
Далее интерпретируем ! в начале строки как заголовок соответствующего уровня. Это тоже несложно.
А вот списки уже сложнее. Здесь тонкость заключается в том, что по результатам разметки строки надо будет объединить в группу (список).
