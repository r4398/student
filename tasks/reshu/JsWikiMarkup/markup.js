(function(jQuery) {
    if (typeof(jQuery.reshu) === "undefined") {
        jQuery.reshu = {}
    }

    var plugin

    plugin = jQuery.reshu.markup = function(src_base_path) {
	console.log('форель')
    }

})(jQuery)
